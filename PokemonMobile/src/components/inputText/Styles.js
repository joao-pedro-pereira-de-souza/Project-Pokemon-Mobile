import {StyleSheet} from 'react-native';
import {COLORS , HeightScreen} from '../../Styles'

const styles = StyleSheet.create({

    container:{
       width:'100%',
       padding: HeightScreen * 0.020,
       borderRadius: HeightScreen * 0.005,
       elevation:5
    }
    
});

export default styles;